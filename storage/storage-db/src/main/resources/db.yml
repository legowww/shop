spring:
  config:
    activate:
      on-profile: local

---
spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:mysql://localhost:3306/lock_sys
    username: "root"
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 25
      maximum-pool-size: 25
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  sql:
    init:
      mode: always
      data-locations:
        - classpath:sql/data.sql
        - classpath:sql/schema.sql
      encoding: UTF-8

---
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:mysql://my-shop-legowww.c5w6iau0q80o.ap-northeast-2.rds.amazonaws.com/shop
    username: legowww
    password: gudwls124!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

---
spring:
  config:
    activate:
      on-profile: dev-no-cache

  datasource:
    url: jdbc:mysql://my-shop-legowww.c5w6iau0q80o.ap-northeast-2.rds.amazonaws.com/shop
    username: legowww
    password: gudwls124!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
